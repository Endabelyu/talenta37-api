name: Deploy to VPS

on:
  push:
    branches:
      - main  # Adjust if you deploy from a different branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v2

      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      # Build Docker image using Bun
      - name: Build Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: endabelyu/talenta37-api:latest
          
      - name: Check Secrets
        run: |
          echo "VPS_HOST: ${{ secrets.VPS_HOST }}"
          echo "VPS_USER: ${{ secrets.VPS_USER }}"
      # Deploy to VPS via SSH
      # - name: Deploy to VPS
      #   uses: appleboy/ssh-action@v0.1.3
      #   with:
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     key: ${{ secrets.VPS_SSH_KEY }}
      #     script: |
      #       # Stop existing container if it exists
      #       if [ "$(docker ps -q -f name=talenta37-api)" ]; then
      #         docker stop talenta37-api
      #         docker rm talenta37-api
      #       fi

      #       # Pull the latest image from the registry
      #       docker pull endabelyu/talenta37-api:latest
            
      #       # Run the new container on port 3001
      #       docker run -d --name talenta37-api -p 3001:3000 endabelyu/talenta37-api:latest
